import { StyleSheet } from "react-native";

export default StyleSheet.create({
    iconSize: {
        width: 40,
        height: 40,
        resizeMode: 'cover'
    },
    activityImage: {
        width: 170,
        height: 100,
        resizeMode: 'cover',
        borderRadius: 10,
    }
});